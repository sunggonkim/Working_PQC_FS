# fscrypt Paper Verdict

- Overall pass: `true`
- Parent B1 gate closed: `true`
- Measured row: `false`
- Environment-blocked row: `true`
- Blocking reasons: `noninteractive_sudo_unavailable, kernel_config_fs_encryption_disabled, root_ext4_encrypt_feature_not_enabled, fscrypt_status_nonzero`
- Kernel CONFIG_FS_ENCRYPTION: `not_set`
- Paper marks unavailable: `true`
- Dangerous paper claims: `0`

## Paper fscrypt Lines

- `Paper/10_Discussion_and_Limitations.tex:17` The primitive measurements cannot establish steady-state throughput, energy efficiency, or portability.  The measured frozen matrix covers plaintext, gocryptfs, dm-crypt, and AEGIS-Q; fscrypt is environment-blocked by kernel/filesystem proof and is not reported as a throughput row.  The matrix is not a full fscrypt, fs-verity, dm-integrity, OP-TEE, SPDK, or GPUDirect Storage comparison.  Its role is to expose authenticated-publication cost so the placement/QoS result is not mistaken for a free storage path.
- `Paper/1_Introduction.tex:25` fscrypt & \cmark & \xmark & \xmark & In-kernel file encryption\\
- `Paper/1_Introduction.tex:37` Post-quantum key establishment is relevant to long-lived edge data, but it does not replace symmetric storage encryption.  ML-KEM establishes shared secrets, whereas AES-GCM remains the appropriate mechanism for bulk data records~\cite{mlkem}.  SHA-256 can bind committed-prefix anchor material to persisted metadata~\cite{sha256}.  The systems challenge is therefore to compose ordinary storage invariants---a key hierarchy that survives remount, nonce/generation state that remains safe across crashes, ciphertext durability before metadata publication, and fail-closed recovery---with accelerator placement and shared-resource control.  Table~\ref{tab:capability_matrix} positions that gap against plaintext, gocryptfs, fscrypt, dm-crypt, fs-verity/dm-integrity, TPM/TEE-backed designs, and GPU-storage systems~\cite{fscrypt, dmcrypt, gocryptfs, optee}; the missing cell is storage-visible policy at the edge encryption boundary.
- `Paper/2_Background.tex:7` Secure-storage systems choose a protection boundary as well as a cipher.  Kernel filesystem encryption, user-space encrypted filesystems, block encryption, and TEEs occupy different boundaries and trust assumptions~\cite{fscrypt, gocryptfs, dmcrypt, optee}.  Those systems make encryption deployable at mature kernel, user, block, or hardware boundaries; AEGIS-Q asks a different runtime question at the mounted edge boundary.  It protects files represented by its own backing format and depends on the kernel, FUSE daemon, OpenSSL/liboqs, and the mount credential.  It is therefore evaluated as a placement/QoS/recovery prototype, not as a replacement for fscrypt or dm-crypt.
- `Paper/2_Background.tex:13` AEGIS-Q distinguishes itself by making placement and recovery policy visible at the same boundary as file encryption.  It is not merely gocryptfs/fscrypt plus CUDA/TPM scripts: the research question is whether one mounted runtime can keep authenticated-block publication, storage-visible QoS/admission, optional batched GPU key-plane work, and external freshness checks under one evidence contract.  In that setting, the storage layer exposes policy decisions that conventional encryption layers usually hide.  A FUSE daemon can decide whether a maintenance job is latency-sensitive, batch-shaped, or freshness-critical; it can also fail closed before exposing a file whose envelope, checkpoint, or external anchor is inconsistent.
- `Paper/2_Background.tex:36` GPU acceleration is useful only when it amortizes launch, placement, and synchronization costs.  Prior out-of-core systems such as FlashNeuron and Fastensor focus on moving large learning workloads through storage hierarchies~\cite{flashneuron, fastensor}; their complementary staging perspectives are relevant to the batch boundary considered here~\cite{fastensor, flashneuron}.  GPU-oriented work such as fscrypt-GPU and FlashNeuron motivates separating batch placement from the storage format~\cite{fscrypt_gpu, flashneuron}.  They do not validate this FUSE format or its durability protocol.  Likewise, storage-oriented systems including SnuQS and InfScaler motivate explicit staging but do not make an NVMe/UVM correctness claim for this prototype~\cite{snuqs_storage, infscaler}.  The evaluation therefore asks when a secure-storage runtime should use the CPU fast path, GPU elastic lane, or conservative fallback on the tested platform.
- `Paper/4_Evaluation.tex:30` Figure~\ref{fig:evaluation_summary} is the evaluation spine: AEGIS-Q keeps AES-GCM data on the CPU, uses the GPU only for a mounted maintenance workflow with explicit slack, and recovers bounded SQLite pressure while giving up background bandwidth.  The strict frozen-contract row is a cost boundary, not the headline claim.  Table~\ref{tab:benchmark_workloads} states what each experiment can establish.  The frozen filesystem contract fixes 4~KiB 70/30 random read/write, \texttt{psync}, per-write \texttt{fdatasync}, queue depth~1, one client, a 1~GiB file, fixed mounts/platform, and five repetitions.  A same-profile attribution probe confirms that the strict path still pays data-sidecar, journal-sidecar, and marker/checkpoint durability boundaries per write-publication cycle.  The matched dm-crypt row runs on the same warm-cache contract: median throughput 9.03~MiB/s and median p99/p99.9 latency 264/823~\(\mu\)s; fscrypt remains environment-blocked because \texttt{CONFIG\_FS\_ENCRYPTION} is unset and the root ext4 encrypt feature is unavailable.
- `Paper/4_Evaluation.tex:45` RQ5 Frozen contract & Plain/gocryptfs/dm-crypt/AEGIS-Q warm; fscrypt unavailable by env. proof & fscrypt throughput and cold-cache rows remain unavailable\\
- `Paper/5_Related_Works.tex:5` AEGIS-Q separates storage protection, GPU/storage placement, and PQC key-plane work; it is not a stronger deployed encryption layer.  fscrypt and dm-crypt are mature kernel baselines~\cite{fscrypt, dmcrypt}, and gocryptfs is the closest user-space encrypted-filesystem comparison point~\cite{gocryptfs}.  fs-verity and dm-integrity validate different boundaries; AEGIS-Q instead combines authenticated FUSE records, storage-visible policy, optional GPU maintenance, and external replay checks.
- `Paper/5_Related_Works.tex:7` Prior GPU/storage systems validate different assumptions.  FlashNeuron and Fastensor use SSD staging for learning workloads~\cite{flashneuron, fastensor}; SnuQS and InfScaler target managed capacity~\cite{snuqs_storage, infscaler}, while SnuQS alone is not filesystem-format evidence~\cite{snuqs_storage}.  GPUfs, SPDK, GPUDirect Storage, and fscrypt-GPU optimize movement or throughput, but not this FUSE format's accepted encrypted records, journal mappings, xattrs, and external anchors~\cite{flashneuron, fscrypt_gpu, fastensor}.  Edge pressure is real~\cite{breaking_edge}; OP-TEE/dm-crypt use different boundaries~\cite{optee, dmcrypt}.  The portable idea is measured admission, while CUDA/cuPQC evidence is platform-specific.

## Non-Claims

- AEGIS-Q is faster than fscrypt.
- AEGIS-Q has an fscrypt speedup.
- The paper reports an fscrypt MiB/s row without a measured frozen-contract artifact.
